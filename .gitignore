__pycache__/

# iCloud Drive sync duplicates ("token_usage 2.py"). This repo lives in
# Mobile Documents, so a sync race can drop a numbered copy of any file
# beside the original.
* [0-9].*

# Local agent configuration, design notes and tool state: kept on the
# maintainer's machine, never published.
/CLAUDE.md
/.claude/
/docs/
/.remember/
/.superpowers/

.DS_Store

# Packaging
*.egg-info/
build/
dist/
