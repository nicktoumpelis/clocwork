__pycache__/
first_pass.log
first_pass.pid

# iCloud Drive sync duplicates ("token_usage 2.py"). This repo lives in
# Mobile Documents, so a sync race can drop a numbered copy of any file
# beside the original; nine of them were committed once already.
* [0-9].*
